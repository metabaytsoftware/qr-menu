# qr-menu - Root Environment Variables
# Copy this file to .env and fill in values for local development

# Node environment
NODE_ENV=development

# These are typically set at the app-level (apps/api/.env and apps/web/.env)
# but can be overridden here if needed for monorepo orchestration
